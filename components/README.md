# components

UI 组件目录。

当前核心 UI 仍集中在 `app/page.tsx`。后续建议优先拆分：

- `MoodPicker.tsx`：状态选择按钮组。
- `TaskResultCard.tsx`：抽取结果卡片。
- `ReminderCard.tsx`：每日轻提醒设置。
- `TaskPoolEditor.tsx`：任务池管理。
- `BottomNav.tsx`：首页/后台切换入口。
